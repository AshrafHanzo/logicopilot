DATABASE_URL=sqlite:///./logicopilot.db
CORS_ORIGINS=http://localhost:5173

# OpenAI (extraction prompt generation + extraction pipeline)
OPENAI_API_KEY=sk-proj-REPLACE_ME
OPENAI_MODEL=gpt-4o-mini

# Google Cloud Document AI (OCR) — put the service-account JSON next to this file
GOOGLE_APPLICATION_CREDENTIALS=google-credentials.json
DOCAI_PROJECT_ID=your-gcp-project-id
DOCAI_LOCATION=us
DOCAI_PROCESSOR_ID=your-processor-id
