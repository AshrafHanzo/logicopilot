fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic[email]>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0

# Document rendering (PDF -> page images, no system deps needed)
PyMuPDF>=1.24

# OCR — Google Cloud Document AI
google-cloud-documentai>=2.29

# LLM — OpenAI (prompt generation + extraction pipeline)
openai>=1.30
