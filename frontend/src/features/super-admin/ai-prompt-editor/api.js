import apiClient from "../../../api/client";

// Thin wrappers around the backend endpoints for "AI Transformation Prompt Editor".
// Backend contract: backend/app/modules/super_admin/ai_prompt_editor/schemas.py + routes.py
// TODO(Magesh): implement calls once the backend endpoints are live.

export async function notImplemented() {
  throw new Error("AI Transformation Prompt Editor API not implemented yet — see README.md");
}
