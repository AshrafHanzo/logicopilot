import apiClient from "../../../api/client";

// Thin wrappers around the backend endpoints for "AI Prompt Training Loop & Correction UI".
// Backend contract: backend/app/modules/super_admin/ai_training_ui/schemas.py + routes.py
// TODO(Magesh): implement calls once the backend endpoints are live.

export async function notImplemented() {
  throw new Error("AI Prompt Training Loop & Correction UI API not implemented yet — see README.md");
}
