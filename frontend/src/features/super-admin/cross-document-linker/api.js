import apiClient from "../../../api/client";

// Thin wrappers around the backend endpoints for "Cross-Document Verification Linker".
// Backend contract: backend/app/modules/super_admin/cross_document_linker/schemas.py + routes.py
// TODO(Magesh): implement calls once the backend endpoints are live.

export async function notImplemented() {
  throw new Error("Cross-Document Verification Linker API not implemented yet — see README.md");
}
